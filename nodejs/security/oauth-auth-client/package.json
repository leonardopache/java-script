{
  "name": "client-oauth-authorization",
  "version": "1.0.0",
  "description": "Simple client to retrieve a token from /auth/token authorization server",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config index.js",
    "watch-node": "nodemon src/*",
    "serve-debug": "nodemon --inspect -r dotenv/config index.js"
  },
  "author": "Leonardo Pache",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.1",
    "openid-client": "^5.4.3",
    "request": "^2.88.2"
  }
}
